# 项目协作规则

- 修改源码时同步本地和 GitHub 远端，推送前确认远端分支未出现其他提交；公开发布前核对提交和工作区状态。
- 登录资料、Cookie、数据库、截图原始素材、`.env`、发布中间产物只保存在本机，不提交到公开仓库。文档图片只从明确选定的公开素材复制到 `docs/`。
- 源码在本地与远端保持一致。若未来加入前端构建，本地 `.env` 使用本机地址，生产 `.env.production` 使用已核实的生产地址；部署构建须显式使用 `--mode production`，不能把生产 URL 写入本地 `.env`。
- 官网由用户自行登录。连接现有 Edge/Chrome 时复用原浏览器会话；固定 Edge 使用独立持久资料。检查登录状态和页面验证，不索取密码，不导出 Cookie，不绕过验证码。
- 收藏商品先导入未选候选，由用户手动勾选。卖家从可读商品详情自动识别，只有用户额外勾选卖家时才遍历店铺其他商品。取消商品或读取失败后，日报不能继续把旧快照当成本轮结果。
- 首次快照、缺失指标和不可比时间窗口显示未知；不得把它们记为零增长。网页结构改变时优先给出可核查的错误状态。
