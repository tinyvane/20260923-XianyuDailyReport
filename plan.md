# 闲鱼商品监控日报计划

更新：2026-09-23

## 目标

参考 `参考页面/闲鱼日报.png` 实现本机浏览器工具：用户在官方闲鱼页面自行登录，选择重点关注卖家，采集其商品可见指标，按可选时间窗口生成日报。账号资料、快照只保存在本机。

## 任务与验收

| 编号 | 优先级 | 任务 | 状态 | 验收 |
| --- | --- | --- | --- | --- |
| R1 | P0 | 还原参考图的日报布局并加入操作控件 | 已实现，待实际数据验收 | 状态、概览、浏览前五、重点卖家均有真实数据或明确空态；浏览器宽窄布局可用 |
| R2 | P0 | 接入现有 Chrome 登录会话 | 已实现连接器，待本人 Chrome 验收 | 不新开 Edge 资料；当前 Chrome 登录检测；过期时停止并提示 |
| R3 | P0 | 从 Chrome 当前关注页导入卖家并选择重点 | 已实现，待本人 Chrome 验收 | 只导入可识别的卖家主页链接；可增删、启停、编辑备注；不能把昵称作为主键 |
| R4 | P0 | 商品采集、快照和时间窗口对比 | 已实现，待本人 Chrome 验收 | 采集可见商品；缺字段标未知；首次采集不显示增长；有历史快照才计算差值 |
| R5 | P1 | 打开日报后自动检查和周期采集 | 已实现，待运行验收 | 页面保持打开时按设置周期采集；关闭页面后停止；显示最近结果和错误 |
| R6 | P0 | 本地测试与远端同步 | 待执行 | 验证 API/数据计算/UI；本地与指定远端源码一致，或记录缺少远端目标 |

## 边界

- 不收集密码、不导出 Cookie、不绕过验证码或风控。用户在官方网页完成登录和验证。
- 官方页面结构可能变化；真实账号的关注页和商品指标必须使用本人账号验收。
- 用户指定在 GitHub 创建私有仓库 `tinyvane/20260923-XianyuDailyReport`；完成验证后同步。

## 2026-09-23 实施记录

- 最初尝试独立 Edge 持久资料；用户明确要求改用已经打开的 Chrome。现已移除服务端 Playwright 启动路径，添加 Chrome MV3 扩展与日报页面桥接；启动器仅在标准 Chrome 资料中新开本机日报标签。
- 原 Edge 扫码后的登录会话用闲鱼登录接口核实为有效；旧判断错误地要求 `cookie2` 存在。由于已改用 Chrome，这条 Edge 路径不再作为交付方案。
- 本地页在 1600×1100、390×844 浏览器视口渲染通过，移动宽度无横向溢出；数据库差值测试与前端脚本语法检查通过。Chrome 扩展真实登录、关注列表和商品页仍待用户安装扩展后验收。
