# 项目计划

更新：2026-09-23

## 目标

提供可在 Windows 本机运行的闲鱼收藏商品监控日报。用户在官方浏览器页面自行登录，从收藏页导入候选，手动选择重点商品；程序识别卖家、保存页面可见指标，按所选时间窗口比较快照。

## 发布状态

| 编号 | 优先级 | 工作 | 状态 | 验收依据 |
| --- | --- | --- | --- | --- |
| R1 | P0 | 日报布局与窄屏适配 | 已实现 | 桌面和 390 像素视口无横向溢出；空态、商品榜和卖家区可渲染 |
| R2 | P0 | 现有 Edge/Chrome 扩展连接与固定 Edge 模式 | 现有 Edge 已真实验证；Chrome、固定 Edge 待更多环境验证 | 登录状态明确；复用所选浏览器会话；未登录时停止采集 |
| R3 | P0 | 收藏导入、手动选择、卖家自动关联 | 已实现并真实验证 | 新候选默认未选；只从已选商品的可读详情确认卖家；无需用户填写卖家 URL |
| R4 | P0 | 指标采集、历史快照和时间窗口比较 | 已实现，长期稳定性持续观察 | 只统计当前有效范围；失败商品不拿旧快照冒充本轮结果；缺少基线时不显示虚构增量 |
| R5 | P1 | 打开页面后的自动与定时采集 | 已实现，长时间运行待验收 | 需服务和日报标签页保持运行；自动采集间隔可配置 |
| R6 | P0 | 文档、发布包和公开仓库 | 已完成 v0.1.0 公开发布 | README 可从零安装；发布包不含登录资料或数据库；源码与远端一致 |

## 已验证事实与限制

- 已在真实 Edge 登录会话中验证扩展连接、收藏卡片导入、商品详情读取、卖家名字提取和日报范围过滤。自动识别的卖家只作为所选商品的关联对象；只有额外勾选卖家时才读取其其他商品。
- 商品页可能已删除、空白、加载超时或要求验证。读取失败保留状态和原因，不推断卖家或指标；上一次成功快照保存在本机，但不能作为本轮有效结果。
- 增量需要前后两次成功快照跨过所选对比窗口。首次采集及数据不足时显示未知。
- Chrome 与固定 Edge 模式尚缺独立账号的完整验收；不能把现有 Edge 的验证结果等同于全部浏览器模式。

## 后续

1. 继续跟踪网页结构变化和长期定时采集；出现页面验证或空白时保持明确失败状态。
2. 在更多账号环境验证 Chrome 与固定 Edge 模式，再决定是否提供独立可执行文件。

## v0.1.0 发布验收

- 私有仓库已切换为 Public；未登录访问仓库返回 HTTP 200。按维护者选择，当前不附加 LICENSE。
- Git 历史中未发现数据库、浏览器资料、原始参考图或常见凭据格式；公开头图单独复制到 `docs/readme-header.jpg`，其原始文件虽以 `.png` 命名，实际为 JPEG，公开路径和格式已匹配。
- 发布包只含 13 个运行与说明文件，解压后在隔离目录启动本机服务，`/api/report`、首页和前端脚本均返回成功。完整首次依赖下载仍取决于用户的 Python 与网络环境。
- [v0.1.0 发布页](https://github.com/tinyvane/20260923-XianyuDailyReport/releases/tag/v0.1.0)已公开；ZIP 从未登录链接下载返回 HTTP 200，大小 728174 字节，SHA-256 为 `9ca73f7981b6759a3467c688276d07301ee3895627ae23928c04b44bb686f731`。公开头图返回 HTTP 200。
