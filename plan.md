# 闲鱼商品监控日报计划

更新：2026-09-23

## 目标

参考 `参考页面/闲鱼日报.png` 实现本机浏览器工具：用户在官方闲鱼页面自行登录，从“我的收藏 → 有效宝贝”选择重点商品，采集详情并识别卖家，按可选时间窗口生成日报。账号资料、快照只保存在本机。

## 任务与验收

| 编号 | 优先级 | 任务 | 状态 | 验收 |
| --- | --- | --- | --- | --- |
| R1 | P0 | 还原参考图的日报布局并加入操作控件 | 已实现，待实际数据验收 | 状态、概览、浏览前五、重点卖家均有真实数据或明确空态；浏览器宽窄布局可用 |
| R2 | P0 | 显式选择现有 Edge、固定 Edge 或 Chrome | 现有 Edge 扩展实测可用；Chrome 和固定 Edge 待账号验收 | 复用所选会话；页面显示登录时停止并提示 |
| R3 | P0 | 从“我的收藏”导入候选并手动选择重点 | 已导入 200 件；待用户勾选重点 | 新候选默认不选；可搜索、筛选、逐件或批量选中；勾选后自动从详情识别卖家名字，无需用户输入 URL |
| R4 | P0 | 重点商品采集、卖家识别、快照和时间窗口对比 | 25 件有效、24 件失效；待所选重点商品验收 | 只采集所选商品；卖家自动关联但不扩展店铺；缺字段标未知；有历史快照才计算差值 |
| R5 | P1 | 打开日报后自动检查和周期采集 | 代码已实现；真实定时运行待验收 | 页面保持打开时按设置周期采集；关闭页面后停止；显示最近结果和错误 |
| R6 | P0 | 本地测试与远端同步 | 11 项测试与浏览器流程检查通过；待推送核对 | 验证 API/数据计算/UI；本地与指定远端源码一致 |

## 边界

- 不收集密码、不导出 Cookie、不绕过验证码或风控。用户在官方网页完成登录和验证。
- 官方页面结构可能变化；真实账号的收藏页和商品指标必须使用本人账号验收。
- 用户指定在 GitHub 创建私有仓库 `tinyvane/20260923-XianyuDailyReport`；完成验证后同步。

## 2026-09-23 实施记录

- 最初尝试独立 Edge 持久资料；用户指出应复用既有浏览器会话。随后用户澄清 Chrome 当时未打开，要求做成浏览器选择。现提供“现有 Edge · 扩展”“固定 Edge · 已扫码”“Chrome · 扩展”，切换需在顶部明确选择。
- Edge 扫码后的资料曾通过闲鱼登录接口验证，且连续两次连接的 10 个相关 Edge 进程 PID 集合完全相同。后来实际收藏页出现扫码弹窗而接口仍返回成功，证明单独接口会误判；登录检查已增加页面可见“登录”按钮判断。
- 本地页在 1600×1100、390×844 浏览器视口渲染曾通过；改动后的移动宽度需复核。数据库差值测试与前端脚本语法检查通过。现有 Edge 扩展已由用户加载并成功读取真实登录会话。
- 私有 GitHub 仓库 `tinyvane/20260923-XianyuDailyReport` 已创建；首个提交 `bb7328dc78c6d75b423fb9b2e9ecd85a96e13e69` 已推送并核对与远端 `main` 一致。浏览器选择实现正在形成下一提交。
- 用户提供网页截图，确认“我的收藏 → 有效宝贝”是可见入口，采集主线改为收藏商品。商品页识别的卖家进入候选名单，可单独选为重点卖家。
- 固定 Edge 资料在真实收藏页曾显示扫码弹窗；用户加载扩展后通过现有 Edge 会话读取了官方收藏页，并导入 200 件候选。真实商品详情有 25 件有效、24 件明确失效；连续详情空白后暂停，未把未完成项当成有效。
- 用户要求先手动勾选几件重点商品，再由程序自动提取卖家名字作为重点店铺。已改为候选默认未选、勾选后立即读取商品详情并关联卖家，无需手输卖家 URL；单独监控店铺其他商品需要额外勾选卖家。历史自动选中的 180 件已清空，后续新增 20 件也默认未选；真实库现为 200 件候选、0 件被选。
- 本轮 11 项 Python 测试、扩展指标解析测试、Python 编译和前端 JS 语法检查通过；浏览器隔离流程验证了“勾选商品 → 自动读取详情 → 提交卖家名字和主页 → 重点卖家显示”。200 件真实候选的桌面和 390 像素手机布局无横向溢出。所选真实商品的卖家自动识别仍待用户勾选后验收；私有仓库同步待完成。
- 本轮源码提交 `23386978a417f03c4cfec0ac2e6a43fdf3e2a23e` 已推送并核对与 GitHub 私有仓库远端 `main` 一致；后续真实采集调整继续同步。
